# see https://github.com/Homebrew/homebrew-bundle
# `cask` and `mas` entries are automatically skipped on Linux
tap "homebrew/bundle"
tap "homebrew/core"
tap "homebrew/cask"
brew "gcc" if OS.linux? # recommended by the install script
brew "zsh" if OS.linux?
brew "antigen"
brew "shellcheck"
cask "google-chrome"
cask "slack"
cask "visual-studio-code"
